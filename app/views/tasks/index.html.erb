
<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <%= link_to "Description", task_path(id:task.id)%>
  <p></p>
  <p>------------------</p>
<%end%>

<%= link_to "Créer une tâche", tasks_new_path %>
